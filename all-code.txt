================================================================================
FILE: app/page.tsx
================================================================================

'use client'

import { useEffect, useRef, useState } from 'react'
import Link from 'next/link'
import Navbar from '@/components/Navbar'
import HeroSection from '@/components/HeroSection'
import Footer from '@/components/Footer'

// ─── Data ─────────────────────────────────────────────────────────────────────

const QUICK_PROMPTS = [
  'Build my full routine',
  'Date night, make me unforgettable',
  'Help, my makeup melts by noon',
  "What's trending right now",
  'Cover my dark circles',
  'Find my perfect shade',
  'Everyday glow up',
  'Dupes for my expensive faves',
]

const LUMI_PROMPTS = [
  "what is trending on TikTok this week?",
  "find me a dupe for Charlotte Tilbury...",
  "what blush works for warm undertones?",
  "build me a clean girl routine",
  "what makeup for a job interview?",
  "what look works for my skin tone?",
]

const OCCASIONS = [
  'do a no-makeup makeup look',
  'get me ready for date night',
  'a polished look for work',
  'glam me up for a night out',
  'build me an everyday look',
]


// ─── Page ─────────────────────────────────────────────────────────────────────

export default function LandingPage() {
  const [count1, setCount1]         = useState(0)
  const [count2, setCount2]         = useState(0)
  const [animated, setAnimated]     = useState(false)
  const [lumiInput, setLumiInput]   = useState('')
  const [lumiResponse, setLumiResponse] = useState('')
  const [lumiLoading, setLumiLoading]   = useState(false)
  const [promptIndex, setPromptIndex]   = useState(0)
  const [occasionsOpen, setOccasionsOpen] = useState(false)
  const [hoverStar, setHoverStar] = useState(0)
  const [rating, setRating]       = useState(0)
  const statsRef = useRef<HTMLDivElement>(null)

  useEffect(() => {
    const observer = new IntersectionObserver(entries => {
      if (entries[0].isIntersecting && !animated) {
        setAnimated(true)
        let s1 = 0
        const t1 = setInterval(() => { s1 += 0.1; if (s1 >= 2) { s1 = 2; clearInterval(t1) } setCount1(Math.round(s1 * 10) / 10) }, 50)
        let s2 = 0
        const t2 = setInterval(() => { s2 += 2; if (s2 >= 100) { s2 = 100; clearInterval(t2) } setCount2(s2) }, 30)
      }
    }, { threshold: 0.3 })
    if (statsRef.current) observer.observe(statsRef.current)
    return () => observer.disconnect()
  }, [animated])

  useEffect(() => {
    const interval = setInterval(() => setPromptIndex(p => (p + 1) % LUMI_PROMPTS.length), 2500)
    return () => clearInterval(interval)
  }, [])

  useEffect(() => {
    if (!occasionsOpen) return
    const close = () => setOccasionsOpen(false)
    document.addEventListener('click', close)
    return () => document.removeEventListener('click', close)
  }, [occasionsOpen])

  const handleLumiAsk = async (override?: string) => {
    const text = override ?? lumiInput
    if (!text.trim()) return
    setLumiLoading(true)
    try {
      const res = await fetch('/api/chat', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ messages: [{ role: 'user', content: text }] }) })
      const data = await res.json()
      setLumiResponse(data.message)
    } catch { setLumiResponse('Something went wrong. Try again.') }
    finally { setLumiLoading(false) }
  }

  const handleQuickPrompt = (p: string) => { setLumiInput(p); handleLumiAsk(p) }

  return (
    <>
      <style>{`
        html { scroll-behavior: smooth; }
        body { background: #FFF8F5; color: #2D1F1A; font-family: var(--font-space-grotesk); }

        @keyframes glamPulse {
          0%, 100% { opacity: 1; transform: scale(1); }
          50%       { opacity: 0.35; transform: scale(0.75); }
        }
        @keyframes glamFloat {
          0%, 100% { transform: translateY(0px); }
          50%       { transform: translateY(-4px); }
        }

        .test-card { transition: border-color 0.2s, box-shadow 0.2s; }
        .test-card:hover { border-color: rgba(232, 113, 74, 0.4) !important; }

        .lumi-list { display: flex; flex-direction: column; }
        .lumi-row {
          position: relative; display: flex; align-items: center; gap: 14px;
          width: 100%; text-align: left; background: none; border: none;
          font-family: var(--font-space-grotesk); font-size: 15px;
          color: rgba(255,255,255,0.9);
          padding: 14px 4px 14px 16px;
          border-bottom: 1px solid rgba(255,255,255,0.22);
          cursor: pointer; transition: all 0.25s;
        }
        .lumi-row:last-child { border-bottom: none; }
        .lumi-row .lumi-bar {
          position: absolute; left: 0; top: 50%; transform: translateY(-50%);
          width: 3px; height: 0; border-radius: 3px;
          background: linear-gradient(180deg, #FFE08A, #E86A9C);
          transition: height 0.25s;
        }
        .lumi-row .lumi-num {
          font-family: var(--font-syne); font-weight: 800; font-size: 15px;
          width: 26px; flex-shrink: 0;
          background: linear-gradient(180deg, #FFE08A, #FFB347);
          -webkit-background-clip: text; background-clip: text;
          -webkit-text-fill-color: transparent;
        }
        .lumi-row .lumi-arrow {
          margin-left: auto; opacity: 0; transform: translateX(-6px);
          transition: all 0.25s;
        }
        .lumi-row:hover { color: #fff; padding-left: 22px; }
        .lumi-row:hover .lumi-bar { height: 60%; }
        .lumi-row:hover .lumi-arrow { opacity: 0.9; transform: translateX(0); }
        .ghost-card:hover { background: rgba(255,255,255,0.26) !important; border-color: rgba(255,255,255,0.8) !important; }

        @keyframes typingBounce {
          0%, 60%, 100% { opacity: 0.3; transform: translateY(0); }
          30%            { opacity: 1;   transform: translateY(-3px); }
        }

        @media (max-width: 768px) {
          .test-grid  { grid-template-columns: 1fr !important; }
          .chat-cols  { flex-direction: column !important; }
          .stats-row  { flex-direction: column !important; gap: 32px !important; }
          .stats-div  { display: none !important; }
        }
      `}</style>

      <div style={{ background: '#FFF8F5', color: '#2D1F1A', fontFamily: 'var(--font-space-grotesk)', overflowX: 'hidden' }}>
        <Navbar />
        <HeroSection />

        {/* LUMI CHAT — warm golden gradient section */}
        <section id="lumi-chat" style={{ background: 'linear-gradient(195deg, rgba(255,225,200,0) 8%, rgba(255,225,200,0.9) 15%, rgba(255,225,200,0.4) 30%, rgba(255,225,200,0) 48%), linear-gradient(160deg, #FFE1C8 0%, #FFC04D 18%, #F0708A 62%, #A24BC7 100%)', padding: '80px 48px 100px', position: 'relative', overflow: 'hidden' }}>
          <div aria-hidden="true" style={{
            position: 'absolute', top: '-120px', right: '-100px',
            width: '620px', height: '620px', borderRadius: '50%',
            background: 'radial-gradient(circle, rgba(165,75,200,0.45), transparent 65%)',
            filter: 'blur(40px)', pointerEvents: 'none', zIndex: 1,
          }} />
          <div style={{ maxWidth: '1100px', margin: '0 auto', position: 'relative', zIndex: 2 }}>
            <div className="chat-cols" style={{ display: 'flex', gap: '60px', alignItems: 'flex-start' }}>

              {/* Left column */}
              <div style={{ flex: '1 1 0', minWidth: 0 }}>
                <p style={{
                  fontFamily: 'var(--font-space-grotesk)', fontSize: '11px', fontWeight: 600,
                  letterSpacing: '0.15em', textTransform: 'uppercase', marginBottom: '16px',
                  display: 'flex', alignItems: 'center', gap: '10px',
                }}>
                  <span aria-hidden="true" style={{ color: '#B14A2A' }}>✦</span>
                  <span style={{ color: '#B14A2A' }}>Your Beauty BFF</span>
                  <span aria-hidden="true" style={{ color: '#B14A2A' }}>✦</span>
                </p>
                <h2 style={{ fontFamily: 'Georgia, serif', fontWeight: 400, fontStyle: 'italic', fontSize: 'clamp(32px, 4vw, 52px)', color: '#FFFFFF', lineHeight: 1.1, letterSpacing: '-0.01em', marginBottom: '18px' }}>Ready to serve your look?</h2>
                <p style={{ fontSize: '16px', color: '#B14A2A', lineHeight: 1.65, marginBottom: '32px', maxWidth: '380px' }}>
                  LUMI is your AI bestie for makeup. Ask away and get answers built for you.
                </p>
                {/* Quick prompt list */}
                <div className="lumi-list" style={{ marginBottom: '36px' }}>
                  {['build my full routine', "what's trending right now", 'find my perfect shade', 'cover my dark circles', 'dupes for my expensive faves'].map((p, i) => (
                    <button key={p} className="lumi-row" onClick={() => handleQuickPrompt(p)}>
                      <span className="lumi-bar" aria-hidden="true" />
                      <span className="lumi-num" aria-hidden="true">{String(i + 1).padStart(2, '0')}</span>
                      {p}
                      <span className="lumi-arrow" aria-hidden="true">→</span>
                    </button>
                  ))}
                </div>
                <Link href="/glam-lab#how-it-works" style={{
                  background: '#E8714A',
                  color: 'white', borderRadius: '999px', padding: '15px 28px',
                  fontSize: '14px', fontWeight: 600, fontFamily: 'var(--font-space-grotesk)',
                  textDecoration: 'none', display: 'inline-block',
                  transition: 'background 0.2s ease',
                  border: '1px solid #C95A35',
                  boxShadow: '0 6px 20px rgba(232,113,74,0.4)',
                  boxSizing: 'border-box',
                }}>Open LUMI Studio</Link>
              </div>

              {/* Right column — dark chat card */}
              <div style={{ flex: '1 1 0', minWidth: 0, maxWidth: '480px' }}>
                <div style={{
                  background: 'linear-gradient(155deg, #3A2330, #281A26)',
                  backdropFilter: 'blur(20px)', WebkitBackdropFilter: 'blur(20px)',
                  border: '1px solid rgba(255,255,255,0.15)',
                  borderRadius: '24px', padding: '24px', display: 'flex', flexDirection: 'column', gap: '16px',
                  position: 'relative', overflow: 'hidden',
                }}>
                  <div aria-hidden="true" style={{ position: 'absolute', width: '260px', height: '260px', borderRadius: '50%', background: 'radial-gradient(circle, rgba(162,75,199,0.4), transparent 70%)', top: '-70px', right: '-50px', filter: 'blur(34px)', pointerEvents: 'none' }} />
                  <div aria-hidden="true" style={{ position: 'absolute', width: '200px', height: '200px', borderRadius: '50%', background: 'radial-gradient(circle, rgba(232,113,74,0.3), transparent 70%)', bottom: '-60px', left: '-30px', filter: 'blur(30px)', pointerEvents: 'none' }} />

                  {/* Header */}
                  <div style={{ display: 'flex', alignItems: 'center', gap: '10px', paddingBottom: '16px', borderBottom: '1px solid rgba(255,255,255,0.1)', position: 'relative', zIndex: 1 }}>
                    <div style={{
                      width: '36px', height: '36px', borderRadius: '50%', flexShrink: 0,
                      background: 'linear-gradient(135deg, #A24BC7, #E86A9C)',
                      display: 'flex', alignItems: 'center', justifyContent: 'center',
                      fontFamily: 'var(--font-syne)', fontWeight: 700, fontSize: '18px', color: 'white',
                      boxShadow: '0 0 16px rgba(200,80,160,0.55)',
                    }}>L</div>
                    <span style={{ fontSize: '14px', fontWeight: 600, color: '#FFFFFF' }}>LUMI</span>
                    <span aria-hidden="true" style={{
                      marginLeft: 'auto', width: '10px', height: '10px', borderRadius: '50%',
                      background: '#2BE06A', display: 'block',
                      boxShadow: '0 0 10px 2px rgba(43,224,106,0.9)',
                      animation: 'glamPulse 2s ease-in-out infinite',
                    }} />
                  </div>

                  {/* Message area */}
                  <div style={{ minHeight: '100px', position: 'relative', zIndex: 1 }}>
                    {!lumiResponse ? (
                      <div style={{ display: 'flex', flexDirection: 'column', gap: '12px', minHeight: '300px', position: 'relative', zIndex: 1 }}>

                        {/* Lumi greeting 1 */}
                        <div style={{ display: 'flex', gap: '8px', alignItems: 'flex-end' }}>
                          <div aria-hidden="true" style={{ width: '24px', height: '24px', borderRadius: '50%', flexShrink: 0, background: 'linear-gradient(135deg, #A24BC7, #E86A9C)', display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: 'var(--font-syne)', fontWeight: 700, fontSize: '11px', color: '#fff', boxShadow: '0 0 10px rgba(200,80,160,0.5)' }}>L</div>
                          <div style={{ background: 'linear-gradient(135deg, rgba(162,75,199,0.2), rgba(232,106,156,0.13))', border: '1px solid rgba(232,106,156,0.22)', borderRadius: '14px 14px 14px 4px', padding: '12px 15px', boxShadow: '0 4px 16px rgba(0,0,0,0.2)' }}>
                            <p style={{ fontSize: '14px', color: '#fff', lineHeight: 1.6 }}>hey! 👋 i'm LUMI</p>
                          </div>
                        </div>

                        {/* Lumi greeting 2 */}
                        <div style={{ display: 'flex', gap: '8px', alignItems: 'flex-end' }}>
                          <div aria-hidden="true" style={{ width: '24px', flexShrink: 0 }} />
                          <div style={{ background: 'linear-gradient(135deg, rgba(162,75,199,0.2), rgba(232,106,156,0.13))', border: '1px solid rgba(232,106,156,0.22)', borderRadius: '14px 14px 14px 4px', padding: '12px 15px', boxShadow: '0 4px 16px rgba(0,0,0,0.2)' }}>
                            <p style={{ fontSize: '14px', color: '#fff', lineHeight: 1.6 }}>ask me anything — dupes, shade matches, routines, trending looks</p>
                          </div>
                        </div>

                        {/* User message (right-aligned) */}
                        <div style={{ display: 'flex', justifyContent: 'flex-end' }}>
                          <div style={{ background: 'rgba(255,255,255,0.14)', border: '1px solid rgba(255,255,255,0.18)', borderRadius: '14px 14px 4px 14px', padding: '12px 15px', maxWidth: '78%' }}>
                            <p style={{ fontSize: '14px', color: 'rgba(255,255,255,0.95)', lineHeight: 1.6 }}>find me a dupe for Charlotte Tilbury flawless filter</p>
                          </div>
                        </div>

                        {/* Lumi reply */}
                        <div style={{ display: 'flex', gap: '8px', alignItems: 'flex-end' }}>
                          <div aria-hidden="true" style={{ width: '24px', height: '24px', borderRadius: '50%', flexShrink: 0, background: 'linear-gradient(135deg, #A24BC7, #E86A9C)', display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: 'var(--font-syne)', fontWeight: 700, fontSize: '11px', color: '#fff', boxShadow: '0 0 10px rgba(200,80,160,0.5)' }}>L</div>
                          <div style={{ background: 'linear-gradient(135deg, rgba(162,75,199,0.2), rgba(232,106,156,0.13))', border: '1px solid rgba(232,106,156,0.22)', borderRadius: '14px 14px 14px 4px', padding: '12px 15px', boxShadow: '0 4px 16px rgba(0,0,0,0.2)' }}>
                            <p style={{ fontSize: '14px', color: '#fff', lineHeight: 1.6 }}>ok this one's easy 👀 e.l.f. halo glow is the go-to dupe — same dewy finish for a fraction of the price. want me to build a full routine around it?</p>
                          </div>
                        </div>

                      </div>
                    ) : (
                      <div>
                        <div style={{ background: 'linear-gradient(135deg, rgba(162,75,199,0.2), rgba(232,106,156,0.13))', border: '1px solid rgba(232,106,156,0.22)', borderRadius: '14px 14px 14px 4px', padding: '12px 14px', marginBottom: '12px', boxShadow: '0 4px 16px rgba(0,0,0,0.2)' }}>
                          <p style={{ fontSize: '14px', color: '#fff', lineHeight: 1.6 }}>{lumiResponse.toLowerCase()}</p>
                        </div>
                        <button onClick={() => { setLumiResponse(''); setLumiInput('') }} style={{ background: 'none', border: 'none', cursor: 'pointer', fontSize: '13px', color: 'rgba(255,255,255,0.4)', fontFamily: 'var(--font-space-grotesk)', padding: 0 }}>← Ask another</button>
                      </div>
                    )}
                    {lumiLoading && (
                      <div style={{ display: 'flex', gap: '4px', alignItems: 'center', background: 'rgba(255,255,255,0.08)', borderRadius: '14px', padding: '11px 14px', width: 'fit-content', marginLeft: '32px', marginTop: '9px', position: 'relative', zIndex: 1 }}>
                        {[0, 0.2, 0.4].map((d, i) => (
                          <span key={i} style={{ width: '6px', height: '6px', borderRadius: '50%', background: 'rgba(232,106,156,0.8)', display: 'block', animation: `typingBounce 1.4s ${d}s infinite` }} />
                        ))}
                      </div>
                    )}
                  </div>

                  {/* Input bar */}
                  <div style={{ position: 'relative' }} onClick={e => e.stopPropagation()}>
                    {occasionsOpen && (
                      <div style={{
                        position: 'absolute', bottom: 'calc(100% + 12px)', left: 0, width: '290px',
                        background: 'rgba(34,22,18,0.98)', border: '1px solid rgba(255,255,255,0.16)',
                        borderRadius: '18px', padding: '8px', boxShadow: '0 12px 40px rgba(0,0,0,0.45)',
                        backdropFilter: 'blur(20px)', WebkitBackdropFilter: 'blur(20px)', zIndex: 10,
                      }}>
                        <p style={{ fontSize: '10px', fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase', color: 'rgba(232,106,156,0.9)', margin: '6px 10px 6px' }}>Pick an occasion</p>
                        {OCCASIONS.map(label => (
                          <button key={label} onClick={() => { setOccasionsOpen(false); handleLumiAsk(label) }} style={{
                            display: 'block', width: '100%', textAlign: 'left',
                            background: 'none', border: 'none', color: 'rgba(255,255,255,0.88)',
                            fontFamily: 'var(--font-space-grotesk)', fontSize: '14px', padding: '10px 12px',
                            borderRadius: '10px', cursor: 'pointer',
                          }}>{label}</button>
                        ))}
                      </div>
                    )}
                    <div style={{ display: 'flex', gap: '8px', alignItems: 'center', background: 'rgba(255,255,255,0.12)', border: '1px solid rgba(255,255,255,0.22)', borderRadius: '100px', padding: '8px' }}>
                      <button
                        onClick={() => setOccasionsOpen(o => !o)}
                        aria-label="Pick an occasion"
                        style={{
                          width: '36px', height: '36px', flexShrink: 0, borderRadius: '50%', border: 'none',
                          background: occasionsOpen ? 'linear-gradient(135deg, #A24BC7, #E86A9C)' : 'rgba(255,255,255,0.14)',
                          color: '#fff', fontSize: '22px', lineHeight: 1, cursor: 'pointer',
                          display: 'flex', alignItems: 'center', justifyContent: 'center',
                          transform: occasionsOpen ? 'rotate(45deg)' : 'rotate(0deg)',
                          transition: 'transform 0.2s, background 0.2s',
                        }}
                      >+</button>
                      <input
                        type="text"
                        value={lumiInput}
                        onChange={e => setLumiInput(e.target.value)}
                        onKeyDown={e => e.key === 'Enter' && handleLumiAsk()}
                        placeholder={LUMI_PROMPTS[promptIndex]}
                        style={{ flex: 1, background: 'transparent', border: 'none', outline: 'none', fontSize: '14px', color: 'rgba(255,255,255,0.75)', fontFamily: 'var(--font-space-grotesk)', caretColor: '#E8714A' }}
                      />
                      <button onClick={() => handleLumiAsk()} disabled={lumiLoading || !lumiInput.trim()} style={{
                        background: 'linear-gradient(135deg, #A24BC7, #E86A9C)',
                        color: 'white', border: 'none', borderRadius: '100px',
                        padding: '9px 18px', fontSize: '13px', fontWeight: 600,
                        fontFamily: 'var(--font-space-grotesk)', cursor: 'pointer', flexShrink: 0,
                        opacity: (lumiLoading || !lumiInput.trim()) ? 0.45 : 1, transition: 'opacity 0.2s',
                      }}>{lumiLoading ? '...' : 'Ask'}</button>
                    </div>
                  </div>

                </div>
              </div>

            </div>
          </div>
        </section>

        {/* STATS */}
        <section ref={statsRef} style={{
          background: '#FFF8F5', padding: '0 48px 72px',
          borderTop: '1px solid rgba(232, 113, 74, 0.18)',
        }}>
          <div style={{ lineHeight: 0, overflow: 'hidden', marginBottom: '40px', marginLeft: '-48px', marginRight: '-48px', height: '60px' }}>
            <svg viewBox="0 0 1440 60" preserveAspectRatio="none" style={{ display: 'block', width: '100%', height: '60px' }}>
              <defs>
                <linearGradient id="waveGrad" x1="0" y1="0" x2="0" y2="1">
                  <stop offset="0%" stopColor="#A24BC7" />
                  <stop offset="100%" stopColor="#A24BC7" />
                </linearGradient>
              </defs>
              <path d="M0,0 L1440,0 L1440,30 C1200,46 1040,40 820,34 C620,28 380,20 0,40 Z" fill="url(#waveGrad)" />
            </svg>
          </div>
          <div className="stats-row" style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', flexWrap: 'wrap', gap: '0' }}>

            <div style={{ textAlign: 'center', padding: '0 72px' }}>
              <div aria-hidden="true" style={{ display: 'flex', justifyContent: 'space-between', width: '90px', margin: '0 auto 14px', color: '#F5A623', fontSize: '14px', opacity: 0.85 }}>
                <span>✦</span><span>✦</span><span>✦</span>
              </div>
              <div style={{ display: 'flex', alignItems: 'baseline', justifyContent: 'center', gap: '4px', marginBottom: '8px' }}>
                <span style={{ fontFamily: 'var(--font-syne)', fontWeight: 800, fontSize: 'clamp(42px, 5vw, 70px)', letterSpacing: '-0.04em', background: 'linear-gradient(180deg, #F9B233 0%, #F2871E 100%)', WebkitBackgroundClip: 'text', backgroundClip: 'text', WebkitTextFillColor: 'transparent', color: 'transparent', lineHeight: 1 }}>{count1}</span>
                <span style={{ fontFamily: 'var(--font-syne)', fontWeight: 600, fontSize: '24px', color: '#F2871E' }}>min</span>
              </div>
              <p style={{ fontSize: '0.65rem', fontWeight: 600, letterSpacing: '0.1em', color: '#E8714A', textTransform: 'uppercase', marginBottom: '6px' }}>To Build Your Routine</p>
            </div>

            <div className="stats-div" aria-hidden="true" style={{ width: '1px', height: '60px', background: 'rgba(232, 113, 74, 0.18)', flexShrink: 0 }} />

            <div style={{ textAlign: 'center', padding: '0 72px' }}>
              <div aria-hidden="true" style={{ display: 'flex', justifyContent: 'space-between', width: '90px', margin: '0 auto 14px', color: '#F5A623', fontSize: '14px', opacity: 0.85 }}>
                <span>✦</span><span>✦</span><span>✦</span>
              </div>
              <div style={{ display: 'flex', alignItems: 'baseline', justifyContent: 'center', gap: '4px', marginBottom: '8px' }}>
                <span style={{ fontFamily: 'var(--font-syne)', fontWeight: 800, fontSize: 'clamp(42px, 5vw, 70px)', letterSpacing: '-0.04em', background: 'linear-gradient(180deg, #F9B233 0%, #F2871E 100%)', WebkitBackgroundClip: 'text', backgroundClip: 'text', WebkitTextFillColor: 'transparent', color: 'transparent', lineHeight: 1 }}>{count2}</span>
                <span style={{ fontFamily: 'var(--font-syne)', fontWeight: 600, fontSize: '24px', color: '#F2871E' }}>%</span>
              </div>
              <p style={{ fontSize: '0.65rem', fontWeight: 600, letterSpacing: '0.1em', color: '#E8714A', textTransform: 'uppercase', marginBottom: '6px' }}>Personalized</p>
            </div>

            <div className="stats-div" aria-hidden="true" style={{ width: '1px', height: '60px', background: 'rgba(232, 113, 74, 0.18)', flexShrink: 0 }} />

            <div style={{ textAlign: 'center', padding: '0 72px' }}>
              <div aria-hidden="true" style={{ display: 'flex', justifyContent: 'space-between', width: '90px', margin: '0 auto 14px', color: '#F5A623', fontSize: '14px', opacity: 0.85 }}>
                <span>✦</span><span>✦</span><span>✦</span>
              </div>
              <div style={{ marginBottom: '8px' }}>
                <span style={{ fontFamily: 'var(--font-syne)', fontWeight: 800, fontSize: 'clamp(42px, 5vw, 70px)', letterSpacing: '-0.04em', background: 'linear-gradient(180deg, #F9B233 0%, #F2871E 100%)', WebkitBackgroundClip: 'text', backgroundClip: 'text', WebkitTextFillColor: 'transparent', color: 'transparent', lineHeight: 1 }}>Free</span>
              </div>
              <p style={{ fontSize: '0.65rem', fontWeight: 600, letterSpacing: '0.1em', color: '#E8714A', textTransform: 'uppercase', marginBottom: '6px' }}>During Beta</p>
            </div>

          </div>
        </section>

        {/* TESTIMONIALS */}
        <section id="community" style={{ background: 'linear-gradient(to bottom, #FFCF40 0%, #FFAD60 45%, #F4845F 100%)', padding: '0 48px 100px', scrollMarginTop: '100px' }}>
          <div style={{ lineHeight: 0, overflow: 'hidden', marginBottom: '80px', marginLeft: '-48px', marginRight: '-48px', height: '60px' }}>
            <svg viewBox="0 0 1440 60" preserveAspectRatio="none" style={{ display: 'block', width: '100%', height: '60px' }}>
              <path d="M0,0 L1440,0 L1440,30 C1200,46 1040,40 820,34 C620,28 380,20 0,40 Z" fill="#FFF8F5"/>
            </svg>
          </div>
          <div style={{ maxWidth: '1100px', margin: '0 auto' }}>
            {/* Header */}
            <div style={{ textAlign: 'center', marginBottom: '48px' }}>
              <p style={{ fontFamily: 'var(--font-space-grotesk)', fontSize: '11px', fontWeight: 600, letterSpacing: '0.14em', color: '#C95A35', textTransform: 'uppercase', marginBottom: '16px' }}>✦ Fresh out of beta ✦</p>
              <h2 style={{ fontFamily: 'var(--font-syne)', fontWeight: 800, fontSize: 'clamp(32px, 4vw, 52px)', lineHeight: 1.05, letterSpacing: '-0.02em', marginBottom: '20px' }}>
                <span style={{ color: '#2D1F1A' }}>Be one of our </span>
                <span style={{ background: 'linear-gradient(135deg, #E8714A 0%, #C95A35 100%)', WebkitBackgroundClip: 'text', backgroundClip: 'text', WebkitTextFillColor: 'transparent' }}>first reviews!</span>
              </h2>
              <p style={{ fontSize: '16px', color: '#7A3C24', lineHeight: 1.6, maxWidth: '520px', margin: '0 auto' }}>ZanZan just launched in beta! The reviews are about to roll in and we'd love yours to be one of the first!</p>
            </div>

            {/* Invite card */}
            <div style={{ background: '#FFFFFF', border: '1px solid rgba(232,113,74,0.15)', boxShadow: '0 8px 40px rgba(232,113,74,0.12)', borderRadius: '28px', padding: '48px 40px', textAlign: 'center', maxWidth: '680px', margin: '0 auto' }}>
              {rating === 0 ? (
                <>
                  <div style={{ display: 'flex', justifyContent: 'center', gap: '8px', marginBottom: '22px' }}>
                    {[1, 2, 3, 4, 5].map(n => (
                      <span
                        key={n}
                        onMouseEnter={() => setHoverStar(n)}
                        onMouseLeave={() => setHoverStar(0)}
                        onClick={() => setRating(n)}
                        style={{
                          fontSize: '34px', lineHeight: 1, cursor: 'pointer',
                          color: (hoverStar || rating) >= n ? '#C455B0' : '#E2D8CE',
                          transform: hoverStar >= n ? 'scale(1.15)' : 'scale(1)',
                          transition: 'transform 0.15s, color 0.15s',
                        }}
                      >★</span>
                    ))}
                  </div>
                  <p style={{ fontFamily: 'var(--font-syne)', fontWeight: 700, fontSize: '26px', color: '#2D1F1A', lineHeight: 1.25, marginBottom: '14px' }}>Tried LUMI? Tell us how it went.</p>
                  <p style={{ fontSize: '15px', color: '#8C6B5E', lineHeight: 1.65, maxWidth: '440px', margin: '0 auto 30px' }}>Share your look, your glow-up, or your honest take. Your review could be what helps someone else decide.</p>
                  <a href="#" style={{ display: 'inline-flex', alignItems: 'center', gap: '8px', background: '#E8714A', color: '#fff', border: '1px solid #C95A35', borderRadius: '999px', padding: '15px 30px', fontSize: '15px', fontWeight: 600, textDecoration: 'none', boxShadow: '0 6px 20px rgba(232,113,74,0.35)' }}>Leave a review →</a>
                </>
              ) : (
                <>
                  <span style={{ display: 'inline-block', fontSize: '13px', fontWeight: 600, letterSpacing: '0.04em', color: '#C95A35', marginBottom: '22px' }}>Thank you for your feedback, we appreciate it!</span>
                  <div style={{ fontSize: '30px', letterSpacing: '6px', marginBottom: '18px', background: 'linear-gradient(135deg, #A24BC7, #E86A9C)', WebkitBackgroundClip: 'text', backgroundClip: 'text', WebkitTextFillColor: 'transparent', color: 'transparent' }}>★★★★★</div>
                  <p style={{ fontFamily: 'var(--font-syne)', fontWeight: 700, fontSize: '25px', color: '#2D1F1A', marginBottom: '10px' }}>Thanks for the love! 💜</p>
                  <p style={{ fontSize: '15px', color: '#8C6B5E', lineHeight: 1.6, maxWidth: '380px', margin: '0 auto' }}>You rated us {rating} {rating === 1 ? 'star' : 'stars'}. Want to tell us more? Drop a full review and help someone else decide.</p>
                  <a href="#" style={{ display: 'inline-flex', alignItems: 'center', gap: '8px', background: '#E8714A', color: '#fff', border: '1px solid #C95A35', borderRadius: '999px', padding: '14px 28px', fontSize: '15px', fontWeight: 600, textDecoration: 'none', boxShadow: '0 6px 20px rgba(232,113,74,0.35)', marginTop: '24px' }}>Write a review →</a>
                </>
              )}
            </div>

            {/* Ghost placeholder cards */}
            <div className="test-grid" style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr', gap: '16px', marginTop: '40px' }}>
              {[
                { text: 'Your review could be here ✨', sub: 'be the first' },
                { text: "This spot's open 💕", sub: 'tell us your take' },
                { text: "You're next ⭐", sub: 'share your glow-up' },
              ].map((g, i) => (
                <div key={i} className="ghost-card" style={{
                  background: 'rgba(255,255,255,0.18)', border: '1px dashed rgba(255,255,255,0.55)',
                  borderRadius: '20px', padding: '32px 24px', minHeight: '170px',
                  display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
                  textAlign: 'center', gap: '12px', transition: 'all 0.2s',
                }}>
                  <div style={{ color: 'rgba(255,255,255,0.65)', fontSize: '16px', letterSpacing: '3px' }}>★★★★★</div>
                  <div style={{ fontFamily: 'var(--font-syne)', fontWeight: 700, fontSize: '19px', color: '#fff', lineHeight: 1.3 }}>{g.text}</div>
                  <div style={{ fontSize: '12.5px', color: 'rgba(255,255,255,0.8)' }}>{g.sub}</div>
                </div>
              ))}
            </div>
          </div>
        </section>

        <Footer />

        {/* FLOATING ASK LUMI — scrolls to embedded chat */}
        <a href="#lumi-chat" style={{
            position: 'fixed', bottom: '28px', right: '28px', zIndex: 200,
            display: 'flex', alignItems: 'center', gap: '10px',
            background: 'linear-gradient(135deg, #A24BC7, #E86A9C)',
            color: 'white', borderRadius: '100px',
            padding: '14px 22px', textDecoration: 'none',
            fontFamily: 'var(--font-space-grotesk)', fontSize: '14px', fontWeight: 600,
            boxShadow: '0 4px 24px rgba(0,0,0,0.25)',
            animation: 'glamFloat 3s ease-in-out infinite',
          }}
        >
          <span aria-hidden="true" style={{ width: '10px', height: '10px', borderRadius: '50%', background: '#2BE06A', display: 'block', flexShrink: 0, boxShadow: '0 0 10px 2px rgba(43,224,106,0.9)', animation: 'glamPulse 2s ease-in-out infinite' }} />
          Ask LUMI
        </a>

      </div>
    </>
  )
}


================================================================================
FILE: components/HeroSection.tsx
================================================================================

'use client'

import Link from 'next/link'

// Hard-coded so positions are identical server + client (no hydration mismatch)
const PARTICLES = [
  { left:  5, color: '#E8714A', size: 3, dur: 14, delay:  0 },
  { left: 12, color: '#FFF8F5', size: 2, dur: 18, delay:  2 },
  { left: 19, color: '#FFCF40', size: 4, dur: 11, delay:  5 },
  { left: 27, color: '#F5A623', size: 3, dur: 16, delay:  1 },
  { left: 33, color: '#E8714A', size: 5, dur: 13, delay:  7 },
  { left: 40, color: '#FFF8F5', size: 2, dur: 20, delay:  3 },
  { left: 46, color: '#FFCF40', size: 4, dur: 15, delay:  9 },
  { left: 53, color: '#E8714A', size: 3, dur: 12, delay:  4 },
  { left: 59, color: '#F5A623', size: 5, dur: 17, delay:  6 },
  { left: 66, color: '#FFF8F5', size: 2, dur: 10, delay:  8 },
  { left: 72, color: '#FFCF40', size: 3, dur: 19, delay:  2 },
  { left: 78, color: '#E8714A', size: 4, dur: 14, delay: 11 },
  { left: 85, color: '#F5A623', size: 2, dur: 16, delay:  5 },
  { left: 91, color: '#FFF8F5', size: 5, dur: 13, delay: 14 },
  { left: 96, color: '#FFCF40', size: 3, dur: 11, delay:  3 },
  { left:  9, color: '#F5A623', size: 4, dur: 18, delay: 13 },
  { left: 24, color: '#E8714A', size: 2, dur: 15, delay:  7 },
  { left: 37, color: '#FFCF40', size: 3, dur: 12, delay: 10 },
  { left: 51, color: '#FFF8F5', size: 5, dur: 20, delay:  4 },
  { left: 64, color: '#F5A623', size: 3, dur: 14, delay: 12 },
  { left: 76, color: '#E8714A', size: 4, dur: 17, delay:  8 },
  { left: 88, color: '#FFCF40', size: 2, dur: 11, delay: 16 },
  { left: 16, color: '#E8714A', size: 3, dur: 13, delay:  6 },
  { left: 44, color: '#F5A623', size: 4, dur: 16, delay: 15 },
  { left: 82, color: '#FFF8F5', size: 5, dur: 19, delay:  9 },
]

export default function HeroSection() {
  return (
    <>
      <style>{`
        @keyframes heroIn {
          from { opacity: 0; transform: translateY(28px); }
          to   { opacity: 1; transform: translateY(0); }
        }
        .hero-1 { animation: heroIn 0.9s ease 0.2s  both; }
        .hero-2 { animation: heroIn 0.9s ease 0.45s both; }
        .hero-3 { animation: heroIn 0.9s ease 0.65s both; }
        .hero-4 { animation: heroIn 0.9s ease 0.85s both; }

        @keyframes glamOrb {
          0%, 100% { transform: scale(1);    opacity: 0.8; }
          50%       { transform: scale(1.18); opacity: 1; }
        }
        @keyframes floatUp {
          0%   { transform: translateY(100vh); opacity: 0; }
          8%   { opacity: 1; }
          92%  { opacity: 0.9; }
          100% { transform: translateY(-120px); opacity: 0; }
        }
        @keyframes heroPulse {
          0%, 100% { opacity: 1; transform: scale(1); }
          50%       { opacity: 0.35; transform: scale(0.75); }
        }
        @keyframes heroDrip {
          0%   { transform: scaleY(0); opacity: 0; }
          40%  { opacity: 1; }
          100% { transform: scaleY(1); opacity: 0; }
        }

@media (prefers-reduced-motion: reduce) {
          .hero-1, .hero-2, .hero-3, .hero-4 { animation: none; opacity: 1; transform: none; }
        }
      `}</style>

      <section style={{
        position: 'relative', minHeight: '100vh', overflow: 'hidden',
        display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
      }}>
        {/* Video */}
        <video
          src="/startup_vid.mp4" autoPlay muted loop playsInline
          style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', objectFit: 'cover', zIndex: 0 }}
        />

        {/* Warm gradient overlay */}
        <div aria-hidden="true" style={{ position: 'absolute', inset: 0, zIndex: 1, background: 'linear-gradient(135deg, rgba(232,113,74,0.3) 0%, rgba(245,166,35,0.2) 100%)' }} />

        {/* Dark vignette */}
        <div aria-hidden="true" style={{ position: 'absolute', inset: 0, zIndex: 2, background: 'radial-gradient(ellipse 70% 80% at 50% 50%, rgba(10,10,10,0.15) 0%, rgba(10,10,10,0.6) 100%)' }} />

        {/* Glow orbs */}
        <div aria-hidden="true" style={{ position: 'absolute', left: '-100px', top: '20%', width: '400px', height: '400px', zIndex: 2, borderRadius: '50%', background: 'radial-gradient(circle, rgba(245,166,35,0.25), transparent 70%)', filter: 'blur(60px)', animation: 'glamOrb 4s ease-in-out infinite' }} />
        <div aria-hidden="true" style={{ position: 'absolute', right: '-100px', bottom: '20%', width: '400px', height: '400px', zIndex: 2, borderRadius: '50%', background: 'radial-gradient(circle, rgba(232,113,74,0.2), transparent 70%)', filter: 'blur(60px)', animation: 'glamOrb 4s ease-in-out 2s infinite' }} />

        {/* Floating particles */}
        {PARTICLES.map((p, i) => (
          <div key={i} aria-hidden="true" style={{
            position: 'absolute', left: `${p.left}%`, bottom: '-10px',
            width: `${p.size}px`, height: `${p.size}px`, borderRadius: '50%',
            background: p.color, zIndex: 4,
            boxShadow: `0 0 ${p.size * 3}px ${p.color}`,
            animation: `floatUp ${p.dur}s linear ${p.delay}s infinite`,
          }} />
        ))}

        {/* Content */}
        <div style={{
          position: 'relative', zIndex: 10,
          display: 'flex', flexDirection: 'column', alignItems: 'center',
          textAlign: 'center', maxWidth: '860px', width: '100%',
          padding: '120px 24px 160px',
        }}>
          {/* Eyebrow label */}
          <p className="hero-1" style={{
            fontFamily: 'var(--font-space-grotesk)',
            fontSize: '15px', fontWeight: 700, letterSpacing: '0.16em',
            textTransform: 'uppercase', marginBottom: '24px',
            display: 'inline-flex', alignItems: 'center', gap: '12px',
            textShadow: '0 1px 8px rgba(0,0,0,0.35)',
          }}>
            <span aria-hidden="true" style={{ color: '#FFD45E', WebkitTextFillColor: '#FFD45E' }}>&#10022;</span>
            <span style={{
              background: 'linear-gradient(135deg, #FFE08A, #FFB347)',
              WebkitBackgroundClip: 'text', backgroundClip: 'text',
              WebkitTextFillColor: 'transparent', color: 'transparent',
            }}>Built by Gen Z</span>
            <span aria-hidden="true" style={{ color: '#FFD45E', WebkitTextFillColor: '#FFD45E' }}>&#10022;</span>
          </p>

          {/* Headline */}
          <h1 className="hero-2" style={{
            fontFamily: 'var(--font-syne)', fontWeight: 800,
            fontSize: 'clamp(52px, 7vw, 96px)', lineHeight: 0.95,
            letterSpacing: '-0.04em', marginBottom: '28px',
          }}>
            <span style={{ display: 'block', color: '#ffffff' }}>Stop guessing.</span>
            <span style={{
              display: 'block',
              background: 'linear-gradient(135deg, #FFCF40 0%, #F5A623 100%)',
              WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', backgroundClip: 'text',
            }}>Start serving.</span>
          </h1>

          {/* Subtext */}
          <p className="hero-3" style={{
            fontSize: '18px', color: 'rgba(255,255,255,0.75)',
            maxWidth: '480px', lineHeight: 1.65, marginBottom: '44px',
            fontFamily: 'var(--font-space-grotesk)', fontWeight: 400,
          }}>
            Every other beauty AI was built to sell you more products. We built one to help you use what you already have.
          </p>

          {/* CTA */}
          <div className="hero-4" style={{ display: 'flex', justifyContent: 'center' }}>
            <Link href="/glam-lab" style={{
              background: '#E8714A',
              color: 'white', borderRadius: '100px', padding: '16px 32px',
              fontSize: '15px', fontWeight: 600,
              fontFamily: 'var(--font-space-grotesk)', letterSpacing: '0.01em',
              textDecoration: 'none', display: 'inline-block',
              boxShadow: '0 4px 20px rgba(232, 113, 74, 0.4)',
              transition: 'all 0.2s ease',
            }}>Find My Look</Link>
          </div>
        </div>

        {/* Scroll hint — See How It Works */}
        <a href="#how-it-works" style={{
          position: 'absolute', bottom: '48px', left: '50%', transform: 'translateX(-50%)',
          display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '8px', zIndex: 10,
          textDecoration: 'none',
        }}>
          <span style={{ fontSize: '11px', color: 'rgba(255,255,255,0.8)', letterSpacing: '0.18em', textTransform: 'uppercase', fontWeight: 600, fontFamily: 'var(--font-josefin)' }}>See How It Works</span>
          <div style={{ width: '1px', height: '40px', background: 'rgba(255,255,255,0.2)', position: 'relative', overflow: 'hidden' }}>
            <div style={{
              position: 'absolute', top: 0, left: 0, right: 0, height: '100%',
              background: 'rgba(255,255,255,0.6)', transformOrigin: 'top',
              animation: 'heroDrip 2s ease-in-out infinite',
            }} />
          </div>
        </a>

        {/* Bottom fade to peach */}
        <div aria-hidden="true" className="absolute bottom-0 left-0 right-0 h-60 pointer-events-none" style={{
          background: 'linear-gradient(to bottom, transparent, #FFE1C8)',
          zIndex: 2,
        }} />
      </section>
    </>
  )
}


================================================================================
FILE: app/api/generate-routine/route.ts
================================================================================

import { NextRequest, NextResponse } from 'next/server'
import OpenAI from 'openai'
import { checkRateLimit } from '@/lib/rateLimit'

const openai = new OpenAI()

type RoutineStep = {
  stepNumber: number
  title: string
  product: string
  technique: string
  placement: string
  tip: string
}

export async function POST(req: NextRequest) {
  try {
    const body = await req.json()
    const { faceAnalysis, products, desiredLook } = body
    const userId = body.userId || null

    const { allowed, resetDate } = await checkRateLimit(userId)
    if (!allowed) {
      return NextResponse.json(
        {
          error: 'limit_reached',
          message: `You've used all 3 free routines this month. Your limit resets on ${resetDate}.`,
          resetDate
        },
        { status: 429 }
      )
    }

    if (!faceAnalysis || !products || !desiredLook) {
      return NextResponse.json(
        { error: 'faceAnalysis, products, and desiredLook are all required' },
        { status: 400 }
      )
    }

    const systemPrompt = `You are an expert professional makeup artist with 15 years
of experience. You give precise, personalized makeup application instructions.
You always consider the user's specific face features when giving placement advice.
You ONLY use products from the user's list — never suggest products they don't have.
You respond exclusively in valid JSON — no markdown, no extra text.`

    const userPrompt = `Create a personalized makeup routine with these details:

FACE ANALYSIS:
${JSON.stringify(faceAnalysis, null, 2)}

PRODUCTS THE USER OWNS:
${products.join(', ')}

DESIRED LOOK: ${desiredLook}

Return a JSON object with this exact structure:
{
  "lookName": "string — creative name for this look",
  "estimatedTime": "string — e.g. '15 minutes'",
  "steps": [
    {
      "stepNumber": 1,
      "title": "string",
      "product": "string — must be from the user's product list",
      "technique": "string — exactly how to apply",
      "placement": "string — precisely where on the face",
      "tip": "string — personalized tip for their specific features"
    }
  ],
  "finishingNotes": "string — final tips for this look"
}`

    const response = await openai.chat.completions.create({
      model: 'gpt-4o',
      max_tokens: 2000,
      temperature: 0.7,
      messages: [
        { role: 'system', content: systemPrompt },
        { role: 'user', content: userPrompt }
      ]
    })

    const rawText = response.choices[0].message.content || '{}'
    const jsonText = rawText.replace(/```json\n?|\n?```/g, '').trim()
    let routine
    try {
      routine = JSON.parse(jsonText)
    } catch {
      return NextResponse.json({ error: 'AI returned an unexpected response format' }, { status: 500 })
    }

    return NextResponse.json({ routine })

  } catch (error) {
    console.error('generate-routine error:', error)
    return NextResponse.json({ error: 'Failed to generate routine' }, { status: 500 })
  }
}


================================================================================
NOTE: The following files were also modified but are too large to include in full:
  - app/glam-lab/page.tsx  (added id="how-it-works", scrollMarginTop, scroll useEffect)
  - components/MakeupWizard.tsx  (Step 1 UI, file handling, routineSavedRef)
  - lib/rateLimit.ts  (userId: string | null, queries by user_id column)
  - app/api/analyze-face/route.ts  (inner try/catch around JSON.parse)
  - app/globals.css  (scroll-behavior: smooth, removed broken font vars)
================================================================================
